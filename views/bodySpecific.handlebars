<!-- 
  Body Specific Handlebars File 
  HTML using MateralizeCSS Design
-->
<main>
  <section id="bodySpecific">
    <div id="index-banner" class="parallax-container parallax-container-sub">
      <div class="section no-pad-bot">
        <div class="container">
          <i class="fas fa-heartbeat logo-sub light-blue-text text-lighten-3">
            <h5 class="companyName header center light-blue-text text-lighten-5">Dipmat Med</h5>
          </i>
          <h4 class="companyName header center light-blue-text text-lighten-5">Symptom Selector - Body Specific Location
          </h4>
          <br><br>

        </div>
      </div>
      <div class="parallax"><img src="/images/ggBridge.png" alt="Unsplashed background img 1"></div>
    </div>

    <!-- 3. Info Container 1 -->
    <div class="container">
      <div class="section">
        <div class="row">
          <div class="input-field col s8">
            <select id="bodySelect">
              <option value="" disabled selected>Choose Area of Symptom</option>
              {{#each locations}}
              <option value="{{ID}}">{{Name}}</option>
              {{/each}}
            </select>
            <label>Body Specific Location Select</label>
            <button class="btn-large waves-effect waves-light disabled" type="submit" name="action"
              id="continueSpec">Continue
              <i class="material-icons right">send</i>
            </button>
          </div>
          <div class="col s4"><img class="materialboxed" id="bodyVector" width="650"
              src="/images/BodyVectors/Empty.png">
          </div>
        </div>
      </div>
  </section>
  <section id="preloader"></section>
</main>

{{> sections/footer}}
<section id="preloader"></section>
{{!-- Front End Js Scripts --}}
{{> scripts/scripts}}
<script src="/js/bodySpec.js"></script>